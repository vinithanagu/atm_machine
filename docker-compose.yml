version: '3.6'

services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: 'atm_machine'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'password'
    ports:
      - 3306:3306

